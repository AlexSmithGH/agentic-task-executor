# Google Cloud / Vertex AI Configuration
GCP_PROJECT_ID=itpc-gcp-hcm-pe-eng-claude
GCP_REGION=us-east5
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json (optional, uses gcloud default if not set)

# GitHub Configuration
GITHUB_TOKEN=your_github_token_here

# Temporal Configuration
TEMPORAL_HOST=localhost:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=agentic-tasks

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Repository Workspace
WORKSPACE_DIR=/tmp/agentic-workspaces
